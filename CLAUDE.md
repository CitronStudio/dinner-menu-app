# 夕食献立アシスタント - 開発メモ

家族用の夕食献立提案アプリ。詳細な経緯・セットアップは README.md を参照。

## アーキテクチャ上の制約（重要）

- **バックエンドなし・API課金なし**が絶対条件。Claude APIやその他の有料外部APIを呼び出す実装は追加しないこと。
- 静的サイト（GitHub Pages配信）のみで完結させる。Cloudflare Workerや他のサーバーサイド実装を復活させないこと（過去に一度、Claude APIで献立を生成する方式を試みたが、課金を避けるため2026-08-17にルールベース方式へ全面的に置き換えた経緯がある）。
- 特売情報はスーパーのサイトから自動取得せず、ユーザーが「今日の特売品」欄に手入力する方式（CORS制約とAI不使用の両方が理由）。自動スクレイピングを足そうとしないこと。

## 構成

```
index.html, css/, js/app.js          … フロントエンド一式
data/seasonal_ingredients.json       … 旬の食材データ（月ごと）
data/recipes.json                    … レシピDB
```

## 提案ロジック（js/app.js）

- 選択した旬の食材＋入力した特売品（カンマ/読点/空白区切り）を、レシピの `main_ingredients` とキーワード一致でスコアリング（特売品マッチ+3、旬食材マッチ+2）。
- マッチ判定はひらがな/カタカナを正規化してから部分一致で比較（`toKatakana` / `matches` 関数）。表記ゆれで拾い漏れないよう、新しいレシピを追加する際も食材名の表記はなるべく `data/seasonal_ingredients.json` や一般的な食材名（豚肉・鶏肉・卵など）に寄せる。
- 条件未入力時はランダムに数件表示するフォールバックがある。

## レシピDBの追加・編集

`data/recipes.json` に以下の形式でオブジェクトを追加する。

```json
{ "id": "r056", "title": "料理名", "summary": "1文程度の説明", "main_ingredients": ["食材1", "食材2"], "category": "炒め物", "time_minutes": 15 }
```

- `id` は既存の連番に続ける形（r001, r002, ...）。
- `main_ingredients` は2〜3個程度、家庭料理として一般的な主要食材に絞る。
- レシピを新規考案する際は、季節感（旬の食材データとの整合）と、家庭で作りやすい手順であることを優先する。奇抜すぎる創作料理より、定番〜やや工夫のある家庭料理を中心に。

## クラウド/リモートセッションでの作業について

このファイルはリポジトリに同梱されているため、iPhone等からclaude.ai/codeで開いたセッションでも読み込まれる。作業後は差分を確認できる形（ブランチ・PR）で反映し、mainへの直接pushは避けること。
